@model Granta.MaterialsWall.Models.PageOfCardsModel
@{
    Layout = "";
}

@if (Model.Cards.Any())
{
    <div id="pagemarker@(Model.PageNumber)" class="col-12">.</div>
    @Html.Partial("_PageOfCards", Model.Cards)
}
